$(function() {
	$('.carousel').carousel({
		interval:2000, // intervalo entre cada imagem
		pause: "null", // quando o mouse estiver em cima, ele continua passando as imagens
		wrap: false // para na ultima foto
	})
});